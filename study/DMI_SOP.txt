Base on DmiEdit5.24


Limitations：
       每个sku 只能先生成一个文件，需要人工贴到同一个report中
       部分在DMIedit 工具中不能读取的值需要另外填写。
1.把dmi log(.txt 格式) 按照sku 命名，并放在Logs文件夹内
2.执行 DMI_v0.1.exe 
3.在logs文件夹下会生成与log 同名的 xls 文件，多个sku 请手动合并。